# 🧪 API-Тесты для Swagger Petstore

Проект содержит автоматизированные **REST API** тесты для [Swagger Petstore API](https://petstore.swagger.io), реализованные с использованием **Java 17**, **JUnit 5**, **Citrus** и **Gradle**.

---

## 🛠 Используемый стек

<p align="center">
<a href="https://www.jetbrains.com/idea/"><img width="10%" title="IntelliJ IDEA" src="media/logo/Intelij_IDEA.svg"></a>
<a href="https://www.java.com/"><img width="10%" title="Java" src="media/logo/Java.svg"></a>
<a href="https://gradle.org/"><img width="10%" title="Gradle" src="media/logo/Gradle.svg"></a>
<a href="https://junit.org/junit5/"><img width="10%" title="JUnit5" src="media/logo/JUnit5.svg"></a>
<a href="https://citrusframework.org/"><img width="10%" title="Citrus" src="media/logo/Citrus.svg"></a>
</p>

<div align="center">

 **Java 17** (Toolchain)  
 **Citrus 3.3.0** (Integration Testing Framework)  
 **JUnit 5** (JUnit Jupiter)  
 **Gradle** (build automation)  
 **Swagger Petstore API**

</div>

---

## 🚀 Обновление: переход на Citrus Framework

Проект был переработан и теперь использует **Citrus** — мощный фреймворк для интеграционного тестирования API. Это позволило:

- отказаться от жёсткой привязки к Rest-assured;
- улучшить читаемость и поддержку сценариев;
- реализовать гибкую работу с переменными и валидацией;
- использовать как позитивные, так и негативные проверки в одном стиле.

---

## 🧩 Что изменилось

### 🆕 Citrus вместо Rest-assured

- ✅ Все тесты были переписаны с использованием **Citrus DSL** (Java, JUnit 5).
- ✅ Поддерживаются `POST`, `GET`, `DELETE` операции.
- ✅ Используется `TestRunner` и аннотация `@CitrusTest`.

### 📁 Конфигурация клиента

Вместо XML-файла используется Java-конфигурация:

```java
// src/test/java/config/CitrusConfig.java
@Configuration
public class CitrusConfig {
    @Bean
    public HttpClient restClient() {
        return CitrusEndpoints.http()
            .client()
            .requestUrl("http://localhost:8080")
            .build();
    }
}
```

---

## 🧭 Нововведения: интеграция с Selenoid и параллельный запуск

### 🧱 Подключение Selenoid

Проект теперь поддерживает запуск тестов через **Selenoid** — контейнеризированную платформу для распределённого запуска браузеров.

```bash
docker-compose up -d
```

- 🌐 Панель управления Selenoid UI: [http://localhost:8080](http://localhost:8080)
- 🧩 Selenium endpoint: `http://localhost:4444/wd/hub`

---

### 🧪 Запуск тестов

#### 💻 Обычный Chrome
```bash
./gradlew clean test -Dbrowser=chrome
```

#### 📱 Chrome Mobile (эмуляция iPhone X)
```bash
./gradlew clean test -Dbrowser=chromeMobile
```

При запуске в мобильном режиме используется эмуляция устройства **iPhone X**, что позволяет проверять адаптивное поведение без реального мобильного девайса.

---

### ⚙️ Параллельное выполнение

В `build.gradle` добавлен блок для распараллеливания выполнения тестов на всех доступных ядрах:

```groovy
test {
    useJUnitPlatform()
    maxParallelForks = Runtime.runtime.availableProcessors().intdiv(2) ?: 1
}
```

Это позволяет выполнять тесты Citrus в несколько потоков, ускоряя общее время выполнения.

---

### 🧩 Конфигурационные файлы

- `docker-compose.yml` — поднимает контейнеры **Selenoid** и **Selenoid UI**  
- `src/test/java/config/SelenoidConfig.java` — Java-конфигурация для **WebDriver (RemoteWebDriver)**  
- `src/test/resources/selenium/selenoid.properties` — параметры окружения и мобильного эмулятора  
- `README_SELENOID.md` — отдельная инструкция для быстрого старта

---

### ✅ Преимущества обновления

- Поддержка **локального Selenoid** без Jenkins/GGR  
- Возможность **мобильного тестирования (iPhone X)**  
- **Параллельный запуск** тестов для ускорения CI/CD  
- Расширяемость под будущие UI-тесты с Citrus Selenium
